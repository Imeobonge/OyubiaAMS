# Normalize all text files to LF in the repo (important for Linux/cPanel deploys
# and for the .cpanel.yml shell script). Windows checkouts still work fine.
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
